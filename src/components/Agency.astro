---
const services = [
  {
    icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>`,
    title: 'Marketing Website Development',
    description:
      'Full-stack sites with headless CMS integration. Designed for speed, SEO, and editor happiness. Delivered from design to deployment — Astro, Next.js, or Svelte depending on what fits.',
  },
  {
    icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><path d="M14 17.5h7M17.5 14v7"/></svg>`,
    title: 'Headless CMS Integration',
    description:
      'Sanity, Contentful, or Storyblok — structured content models, custom schemas, editorial workflows, and preview environments your marketing team will love.',
  },
  {
    icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>`,
    title: 'Performance Optimization Audit',
    description:
      'Core Web Vitals deep-dive for your existing site. Identify bottlenecks, implement fixes, and hit that Lighthouse 100. Measurable results, documented improvements.',
  },
  {
    icon: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/></svg>`,
    title: 'Design System & Component Library',
    description:
      'Scalable, documented component libraries built with Storybook. From design tokens to production-ready atoms — consistent UI that scales across your entire product.',
  },
];

const outcomes = [
  {
    stat: '< 0.4s',
    label: 'Sub-second pages',
    detail: 'LCP under 0.4s, 11KB bundles, Lighthouse 100 — measurable speed on every build.',
    color: '#22c55e',
  },
  {
    stat: '100%',
    label: 'Editors love it',
    detail: 'Structured headless CMS, live preview, real-time collaboration. No engineering bottleneck for content.',
    color: '#6366f1',
  },
  {
    stat: 'SSG',
    label: 'SEO by default',
    detail: 'Static output, structured data, Core Web Vitals green. Google ranks what loads fast.',
    color: '#f59e0b',
  },
  {
    stat: '5+',
    label: 'Stacks supported',
    detail: 'Astro, Next.js, Svelte, React, Vue — matched to your team, your product, your constraints.',
    color: '#06b6d4',
  },
];
---

<section id="studio" class="py-24 px-6 relative overflow-hidden">
  <!-- Animated canvas background -->
  <canvas id="studio-canvas" class="absolute inset-0 w-full h-full pointer-events-none" style="opacity: 0.8;"></canvas>

  <div class="max-w-6xl mx-auto relative">
    <!-- Header -->
    <div class="reveal text-center mb-20">
      <div class="section-label">The Studio</div>
      <h2 class="text-4xl md:text-6xl font-black text-white leading-tight mb-6">
        A boutique studio for<br/>
        <span class="gradient-text">the modern web.</span>
      </h2>
      <p class="text-zinc-400 text-xl max-w-2xl mx-auto leading-relaxed">
        30+ marketing websites shipped across fintech, SaaS, and enterprise.
        Every project gets the right stack — chosen for performance, DX, and long-term maintainability.
      </p>
    </div>

    <!-- Outcomes grid -->
    <div class="reveal mb-20">
      <div class="text-center mb-10">
        <div class="section-label">What you get</div>
        <h3 class="text-2xl font-bold text-white">Outcomes, not just deliverables.</h3>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        {outcomes.map((o, i) => (
          <div class={`reveal reveal-delay-${i + 1} card p-6 group hover:-translate-y-1 transition-transform duration-200`}>
            <div class="text-3xl font-black mb-1" style={`color: ${o.color};`}>{o.stat}</div>
            <div class="text-white font-semibold text-sm mb-2">{o.label}</div>
            <div class="text-zinc-500 text-xs leading-relaxed">{o.detail}</div>
          </div>
        ))}
      </div>
    </div>

    <!-- Services -->
    <div class="reveal">
      <div class="text-center mb-10">
        <div class="section-label">Services</div>
        <h3 class="text-2xl font-bold text-white">What I build for you.</h3>
      </div>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {services.map((s, i) => (
          <div class={`reveal reveal-delay-${(i % 2) + 1} card p-6 group hover:-translate-y-1 transition-transform duration-200`}>
            <div class="w-10 h-10 rounded-xl bg-indigo-500/10 border border-indigo-500/20 flex items-center justify-center text-indigo-400 mb-4 group-hover:bg-indigo-500/20 transition-colors duration-200">
              <Fragment set:html={s.icon} />
            </div>
            <div class="text-white font-bold mb-2">{s.title}</div>
            <div class="text-zinc-400 text-sm leading-relaxed">{s.description}</div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>

<script>
  // Animated node-graph background for the studio section
  const canvas = document.getElementById('studio-canvas') as HTMLCanvasElement | null;
  if (!canvas) throw new Error('no canvas');
  const ctx = canvas.getContext('2d')!;

  const resize = () => {
    canvas.width  = canvas.clientWidth  * devicePixelRatio;
    canvas.height = canvas.clientHeight * devicePixelRatio;
    ctx.scale(devicePixelRatio, devicePixelRatio);
  };
  resize();
  window.addEventListener('resize', resize);

  const NODE_COUNT = 45;
  const W = () => canvas.clientWidth;
  const H = () => canvas.clientHeight;

  const COLORS = ['99,102,241', '139,92,246', '34,211,238', '168,85,247'];

  const nodes = Array.from({ length: NODE_COUNT }, (_, i) => ({
    x: Math.random() * W(),
    y: Math.random() * H(),
    vx: (Math.random() - 0.5) * 0.45,
    vy: (Math.random() - 0.5) * 0.45,
    r: 1.5 + Math.random() * 3,
    color: COLORS[i % COLORS.length],
  }));

  const LINK_DIST = 180;

  const draw = () => {
    requestAnimationFrame(draw);
    ctx.clearRect(0, 0, W(), H());

    const w = W(), h = H();

    nodes.forEach(n => {
      n.x += n.vx;
      n.y += n.vy;
      if (n.x < 0 || n.x > w) n.vx *= -1;
      if (n.y < 0 || n.y > h) n.vy *= -1;
    });

    // Draw edges
    for (let i = 0; i < nodes.length; i++) {
      for (let j = i + 1; j < nodes.length; j++) {
        const dx = nodes[i].x - nodes[j].x;
        const dy = nodes[i].y - nodes[j].y;
        const d  = Math.sqrt(dx * dx + dy * dy);
        if (d < LINK_DIST) {
          const alpha = (1 - d / LINK_DIST) * 0.35;
          ctx.beginPath();
          ctx.moveTo(nodes[i].x, nodes[i].y);
          ctx.lineTo(nodes[j].x, nodes[j].y);
          ctx.strokeStyle = `rgba(${nodes[i].color},${alpha})`;
          ctx.lineWidth = 1;
          ctx.stroke();
        }
      }
    }

    // Draw nodes
    nodes.forEach(n => {
      ctx.beginPath();
      ctx.arc(n.x, n.y, n.r, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(${n.color},0.7)`;
      ctx.fill();
    });
  };
  draw();
</script>
